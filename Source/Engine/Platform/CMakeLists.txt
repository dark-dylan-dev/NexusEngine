# SPDX-License-Identifier: MIT

file(GLOB_RECURSE COMMON_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/Common/*.cpp)
set(PLATFORM_SOURCES)

if(WIN32)
    file(GLOB_RECURSE PLATFORM_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/Windows/*.cpp)
elseif(APPLE)
    file(GLOB_RECURSE PLATFORM_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/MacOS/*.cpp)
elseif(UNIX)
    file(GLOB_RECURSE PLATFORM_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/Linux/*.cpp)
endif()

target_include_directories(NexusEngine PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/Include)
target_sources(NexusEngine PRIVATE ${COMMON_SOURCES} ${PLATFORM_SOURCES})
